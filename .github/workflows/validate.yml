name: validate

on:
  push:
    branches: [main]
  pull_request:

jobs:
  catalog:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate reference catalog and index freshness
        run: skills/solidity-gas-reference-creator/scripts/validate-references.sh
